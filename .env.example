# MongoDB 连接字符串（数据库名直接写在 URI 末尾）
# 本地示例：
MONGODB_URI=mongodb://localhost:27017/nest

# Render / Atlas 示例：
# MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/nest?retryWrites=true&w=majority

# 可选：单独指定数据库名（不填则从 URI 中自动解析）
# MONGODB_DB=nest
